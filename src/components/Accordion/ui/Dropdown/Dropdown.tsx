
interface IDropdown {
    title: string;
    description: string;
}

const Dropdown = (props: IDropdown) => {
    const {title, description} = props;
    return(
        <div></div>
    )
}

export default Dropdown;